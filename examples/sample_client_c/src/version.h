#ifndef SAMPLE_CLIENT_VERSION_H
#define SAMPLE_CLIENT_VERSION_H

#include <connector.h>

static const CrVersion CLIENT_VERSION = {
        .major = 6,
        .minor = 0,
        .patch = 0
};

#endif //SAMPLE_CLIENT_VERSION_H
